// Sum of two numbers
#include<stdio.h>
#include<conio.h>
int main()
{
int a,b,sum;
printf("Enter the first value :");
scanf("%d", &a);
printf("Enter the second value :");
scanf("%d", &b);
sum=a+b;
printf("The result is=%d", sum);
return 0;
}