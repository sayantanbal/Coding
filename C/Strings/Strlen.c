#include<stdio.h>
#include<string.h>
int main()
{
char *st = "Tuhin";
int a = strlen(st);
// This function can count the length of the string excluding the null character
printf("The lengfh of strimg st is %d", a);
return 0;
}